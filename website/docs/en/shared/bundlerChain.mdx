Bundler chain is a subset of webpack chain, which contains part of the webpack chain API. You can use it to modify the builtin Rspack config.

Compared with modifying the Rspack configuration object, bundler-chain is more powerful. It not only supports chain calls, but also allows you to modify of builtin rules or plugins based on ids.
