// This folder disabled `skipLibCheck` to check the public types of @rsbuild/core
/// <reference types="@rsbuild/core/types" />
import { createRsbuild, defineConfig } from '@rsbuild/core';

createRsbuild({});

defineConfig({});
