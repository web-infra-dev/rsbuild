# Quick Start

## Setup Environment

import NodeVersion from '@en/shared/nodeVersion.md';

<NodeVersion />

## Creating an Rsbuild Project

You can use the `create-rsbuild` to create a new Rsbuild project. Just execute the following command:

import { PackageManagerTabs } from '@theme';

<PackageManagerTabs command="create rsbuild@latest" />

Then follow the prompts to complete the operation. You can choose from the following templates provided by `create-rsbuild`:

| Template    | Description                                      |
| ----------- | ------------------------------------------------ |
| react-js    | [React 18](https://react.dev/)                   |
| react-ts    | [React 18](https://react.dev/) and TypeScript    |
| vue3-js     | [Vue 3](https://vuejs.org/)                      |
| vue3-ts     | [Vue 3](https://vuejs.org/) and TypeScript       |
| vue2-js     | [Vue 2](https://v2.vuejs.org/)                   |
| vue2-ts     | [Vue 2](https://v2.vuejs.org/) and TypeScript    |
| svelte-js   | [Svelte](https://svelte.dev/)                    |
| svelte-ts   | [Svelte](https://svelte.dev/) and TypeScript     |
| vanilla-js  | Vanilla JavaScript                               |
| vanilla-ts  | Vanilla TypeScript                               |

## Command Line Interface

Rsbuild comes with a lightweight CLI that includes commands such as `dev` and `build`.

```json
{
  "scripts": {
    // starting the dev server
    "dev": "rsbuild dev",
    // build the app for production
    "build": "rsbuild build",
    // preview the production build locally
    "preview": "rsbuild preview"
  }
}
```

Refer to the [CLI Tool](/guide/basic/cli) to learn about all available commands and options.

## Build Entry

By default, Rsbuild CLI uses `src/index.(js|ts|jsx|tsx)` as the build entry. You can modify the build entry using the `source.entries` configuration option.

- **Type:**

```ts
type Entries = Record<string, string>;
```

- **Default:**

```ts
const defaultEntry = {
  index: 'src/index.(js|ts|jsx|tsx)',
};
```

- **Example:**

```ts title="rsbuild.config.ts"
import { defineConfig } from '@rsbuild/core';

export default defineConfig({
  source: {
    entries: {
      foo: './src/pages/foo/index.ts',
      bar: './src/pages/bar/index.ts',
    },
  },
});
```

## Next Step

You may want:

import NextSteps from '@components/NextSteps';
import Step from '@components/Step';

<NextSteps>
  <Step
    href="/guide/start/features.html"
    title="All Features"
    description="Learn all features of Rsbuild"
  />
  <Step
    href="/guide/basic/config.html"
    title="Config"
    description="Learn how to configure Rsbuild"
  />
  <Step
    href="/guide/start/glossary.html"
    title="Glossary"
    description="Learn about Rsbuild related concepts"
  />
</NextSteps>
