---
extractApiHeaders: [2]
---

# Rsbuild Types

This section describes some of the type definitions provided by the Rsbuild.

## BuilderInstance

The type of the Rsbuild instance.

```ts
import type { BuilderInstance } from '@rsbuild/core';

let builder: BuilderInstance;
```

You can pass in the Provider type through generics to make the type definition of the Rsbuild instance more accurate:

```ts
import type { createRsbuild, type BuilderInstance } from '@rsbuild/core';
import type { BuilderWebpackProvider } from '@rsbuild/webpack-provider';

let builder: BuilderInstance<BuilderWebpackProvider>;
```

## BuilderContext

The type of the context property in the Rsbuild instance.

```ts
import type { BuilderContext } from '@rsbuild/core';

const context: BuilderContext = builder.context;
```

## BuilderPlugin

The type of Rsbuild plugin, should be used with the `BuilderPluginAPI` type exported from the provider.

```ts
import type { BuilderPlugin } from '@rsbuild/core';
import type { BuilderPluginAPI } from '@rsbuild/webpack-provider';

const myPlugin: BuilderPlugin<BuilderPluginAPI> = {
  name: 'my-plugin',
  setup() {},
};
```

## BuilderTarget

The type of build target.

```ts
import type { BuilderTarget } from '@rsbuild/core';
```

## BuilderEntry

The type of the `entry` option to the `createRsbuild` method.

```ts
import type { BuilderEntry } from '@rsbuild/core';
```

## CreateBuilderOptions

The param type of `createRsbuild` method.

```ts
import type { CreateBuilderOptions } from '@rsbuild/core';
```
