{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["./scripts/modern.base.config.ts"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**/*"],
      "inputs": ["src/**/*", "tsconfig.json", "package.json", "modern.config.*"]
    }
  }
}
